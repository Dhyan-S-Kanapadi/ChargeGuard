import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import { AlertTriangle, ChevronLeft, ChevronRight, FileText, RefreshCw, X } from "lucide-react";
import { useMemo, useState } from "react";
import type { DisputeDetail } from "../../api/schemas";
import { useConnection } from "../../app/ConnectionContext";
import { Badge, Button, ConfirmDialog, EmptyState, ErrorState, Panel, Skeleton } from "../../components/ui";
import { deadlineLabel, formatDate, formatMoney, formatPercent, recommendationTone } from "../../utils/format";

const PAGE_SIZE = 10;
function caseFromHash() { return new URLSearchParams(location.hash.split("?")[1] ?? "").get("case") ?? ""; }
export function outcomeEligible(item: DisputeDetail) {
  const s = item.state;
  return item.status === "completed" && s.decision === "FIGHT" && s.quality_approved && Boolean(s.filed_at) && s.filing_confirmation?.startsWith("filed") === true && !s.final_outcome;
}

export function DisputesPage() {
  const { client, selectedMerchantId } = useConnection();
  const queryClient = useQueryClient();
  const query = useQuery({ queryKey: ["disputes", "details"], queryFn: ({ signal }) => client.disputes(signal) });
  const [search, setSearch] = useState(() => new URLSearchParams(location.hash.split("?")[1] ?? "").get("q") ?? "");
  const [decision, setDecision] = useState(""); const [status, setStatus] = useState(""); const [currency, setCurrency] = useState("");
  const [sort, setSort] = useState("deadline"); const [page, setPage] = useState(1); const [selected, setSelected] = useState(caseFromHash);
  const rows = useMemo(() => (query.data ?? []).filter((item) => {
    const haystack = `${item.chargeback_id} ${item.state.merchant_profile.name} ${item.state.reason_code} ${item.state.provider_reason_code ?? ""}`.toLowerCase();
    return (!selectedMerchantId || item.state.merchant_profile.merchant_id === selectedMerchantId) && (!search || haystack.includes(search.toLowerCase())) && (!decision || item.state.decision === decision) && (!status || item.status === status) && (!currency || item.state.currency === currency);
  }).sort((a, b) => sort === "amount" ? b.state.dispute_amount - a.state.dispute_amount : sort === "updated" ? +new Date(b.updated_at) - +new Date(a.updated_at) : +new Date(a.state.filing_deadline) - +new Date(b.state.filing_deadline)), [query.data, search, decision, status, currency, selectedMerchantId, sort]);
  const pages = Math.max(1, Math.ceil(rows.length / PAGE_SIZE)); const visible = rows.slice((Math.min(page, pages) - 1) * PAGE_SIZE, Math.min(page, pages) * PAGE_SIZE);
  if (query.isLoading) return <><Heading /><Skeleton lines={8} /></>;
  if (query.error) return <><Heading /><ErrorState error={query.error} retry={() => void query.refetch()} /></>;
  return <><Heading /><Panel className="table-panel"><div className="filters"><label>Search<input value={search} onChange={(e) => { setSearch(e.target.value); setPage(1); }} placeholder="Case, merchant, reason" /></label><label>Recommendation<select value={decision} onChange={(e) => setDecision(e.target.value)}><option value="">All</option><option>FIGHT</option><option>ACCEPT</option><option>ESCALATE_DEGRADED</option></select></label><label>Processing<select value={status} onChange={(e) => setStatus(e.target.value)}><option value="">All</option>{[...new Set((query.data ?? []).map((x) => x.status))].map(x => <option key={x}>{x}</option>)}</select></label><label>Currency<select value={currency} onChange={(e) => setCurrency(e.target.value)}><option value="">All</option>{[...new Set((query.data ?? []).map((x) => x.state.currency))].map(x => <option key={x}>{x}</option>)}</select></label><label>Sort<select value={sort} onChange={(e) => setSort(e.target.value)}><option value="deadline">Deadline</option><option value="amount">Amount</option><option value="updated">Last updated</option></select></label><Button variant="secondary" onClick={() => void query.refetch()} loading={query.isFetching}><RefreshCw />Refresh</Button></div><p className="filter-summary">{rows.length} of {query.data?.length ?? 0} cases shown{[search, decision, status, currency].filter(Boolean).length ? ` · ${[search, decision, status, currency].filter(Boolean).length} active filters` : ""}</p>{visible.length ? <><div className="table-wrap"><table><thead><tr><th>Case</th><th>Merchant</th><th>Amount</th><th>Processing</th><th>Recommendation</th><th>Evidence</th><th>Confidence</th><th>Deadline</th><th>Updated</th></tr></thead><tbody>{visible.map(item => <tr key={item.chargeback_id} onClick={() => setSelected(item.chargeback_id)}><td><button className="link-button">{item.chargeback_id}</button><small>{item.state.provider_reason_code || item.state.reason_code}</small></td><td>{item.state.merchant_profile.name}</td><td>{formatMoney(item.state.dispute_amount, item.state.currency)}</td><td><Badge>{item.status}</Badge></td><td><Badge tone={recommendationTone(item.state.decision)}>{item.state.decision ?? "PENDING"}</Badge></td><td>{item.state.evidence_collection_degraded ? <span className="evidence-alert"><AlertTriangle />Degraded</span> : "Ready"}</td><td>{formatPercent(item.state.win_probability)}</td><td>{deadlineLabel(item.state.filing_deadline).text}</td><td>{formatDate(item.updated_at)}</td></tr>)}</tbody></table></div><div className="case-cards">{visible.map(item => <button key={item.chargeback_id} onClick={() => setSelected(item.chargeback_id)}><span><strong>{item.chargeback_id}</strong><Badge tone={recommendationTone(item.state.decision)}>{item.state.decision ?? "PENDING"}</Badge></span><b>{formatMoney(item.state.dispute_amount, item.state.currency)}</b><small>{item.state.merchant_profile.name} · {deadlineLabel(item.state.filing_deadline).text}</small></button>)}</div><div className="pagination"><Button variant="ghost" disabled={page <= 1} onClick={() => setPage(page - 1)}><ChevronLeft />Previous</Button><span>Page {Math.min(page, pages)} of {pages}</span><Button variant="ghost" disabled={page >= pages} onClick={() => setPage(page + 1)}>Next<ChevronRight /></Button></div></> : <EmptyState title={query.data?.length ? "No cases match" : "No disputes yet"}>{query.data?.length ? "Clear or adjust the active filters." : "Processed disputes will appear here."}</EmptyState>}</Panel>{selected ? <DetailDrawer id={selected} close={() => setSelected("")} invalidate={() => queryClient.invalidateQueries()} /> : null}</>;
}
function Heading() { return <header className="page-heading"><div><p className="eyebrow">Case workspace</p><h1>Disputes</h1><p>Recommendations, processing state, and final network outcomes remain distinct.</p></div></header>; }

function DetailDrawer({ id, close, invalidate }: { id: string; close: () => void; invalidate: () => void }) {
  const { client } = useConnection(); const detail = useQuery({ queryKey: ["dispute", id], queryFn: ({ signal }) => client.dispute(id, signal) }); const summary = useQuery({ queryKey: ["summary", id], queryFn: ({ signal }) => client.summary(id, signal), enabled: false, retry: false });
  const [confirm, setConfirm] = useState<"WIN" | "LOSS" | "">(""); const [reason, setReason] = useState(""); const [success, setSuccess] = useState("");
  const outcome = useMutation({ mutationFn: (value: "WIN" | "LOSS") => client.recordOutcome(id, value, reason), onSuccess: async () => { setSuccess(`Network outcome recorded for ${id}.`); setConfirm(""); await Promise.all([detail.refetch(), invalidate()]); } });
  return <><button className="drawer-scrim" onClick={close} aria-label="Close case" /><aside className="detail-drawer" aria-label={`Case ${id}`}><header><div><p className="eyebrow">Case detail</p><h2>{id}</h2></div><button className="icon-button" onClick={close} aria-label="Close case"><X /></button></header>{detail.isLoading ? <Skeleton lines={10} /> : detail.error ? <ErrorState error={detail.error} retry={() => void detail.refetch()} /> : detail.data ? <div className="detail-body"><section className="detail-hero"><span><small>Claim amount</small><strong>{formatMoney(detail.data.state.dispute_amount, detail.data.state.currency)}</strong></span><Badge tone={recommendationTone(detail.data.state.decision)}>{detail.data.state.decision ?? "PENDING"} recommendation</Badge></section>{detail.data.state.evidence_collection_degraded ? <div className="warning-box"><AlertTriangle />Evidence collection is degraded: {detail.data.state.degraded_reasons.join(", ") || "reason unavailable"}</div> : null}<dl className="detail-grid"><Field label="Processing status" value={detail.data.status} /><Field label="Network outcome" value={detail.data.state.final_outcome ?? "Not adjudicated"} /><Field label="Confidence" value={formatPercent(detail.data.state.win_probability)} /><Field label="Expected value" value={detail.data.state.expected_value == null ? "Unavailable" : formatMoney(detail.data.state.expected_value, detail.data.state.currency)} /><Field label="Filing deadline" value={formatDate(detail.data.state.filing_deadline)} /><Field label="Filed at" value={formatDate(detail.data.state.filed_at)} /><Field label="Merchant" value={detail.data.state.merchant_profile.name} /><Field label="Reason" value={detail.data.state.provider_reason_code || detail.data.state.reason_code} /><Field label="Payment ID" value={detail.data.state.payment_id ?? "Unavailable"} /><Field label="Order ID" value={detail.data.state.order_id ?? "Unavailable"} /></dl><Section title="Decision reasoning" value={detail.data.state.decision_reasoning} /><Section title="Contradiction summary" value={detail.data.state.contradiction_summary} /><Panel title="Evidence checklist">{["transaction", "shipping", "comms", "device", "consortium", "delivery_photo", "order_timeline"].map(k => <span className="check-item" key={k}>{detail.data!.state[k as keyof typeof detail.data.state] ? "✓" : "—"} {k.replaceAll("_", " ")}</span>)}</Panel><Panel title="Human review summary" action={<Button variant="secondary" onClick={() => void summary.refetch()} loading={summary.isFetching}><FileText />Generate</Button>}>{summary.error ? <ErrorState error={summary.error} retry={() => void summary.refetch()} /> : <p>{summary.data?.human_review_summary ?? detail.data.state.human_review_summary ?? "Generate a grounded summary when needed."}</p>}</Panel>{outcomeEligible(detail.data) ? <Panel title="Record final card-network outcome"><p>This is separate from ChargeGuard’s {detail.data.state.decision} recommendation.</p><div className="button-row"><Button onClick={() => setConfirm("WIN")}>Record WIN</Button><Button variant="danger" onClick={() => setConfirm("LOSS")}>Record LOSS</Button></div></Panel> : null}{success ? <p className="success-box" role="status">{success}</p> : null}{outcome.error ? <ErrorState error={outcome.error} /> : null}</div> : null}</aside><ConfirmDialog open={Boolean(confirm)} title="Confirm final network outcome" confirmLabel={`Record ${confirm}`} danger={confirm === "LOSS"} pending={outcome.isPending} onClose={() => setConfirm("")} onConfirm={() => outcome.mutate(confirm as "WIN" | "LOSS")}><p>Case <strong>{id}</strong> will be recorded as <strong>{confirm}</strong>. This cannot be replaced by a conflicting outcome.</p><label>Reason (optional)<textarea value={reason} onChange={(e) => setReason(e.target.value)} maxLength={2000} /></label></ConfirmDialog></>;
}
function Field({ label, value }: { label: string; value: string }) { return <div><dt>{label}</dt><dd>{value}</dd></div>; } function Section({ title, value }: { title: string; value?: string | null }) { return <section><h3>{title}</h3><p>{value || "Unavailable"}</p></section>; }
